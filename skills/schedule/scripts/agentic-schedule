#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
SKILL_DIR=$(dirname -- "$SCRIPT_DIR")

export AGENTIC_SCHEDULE_WRAPPER="$SCRIPT_DIR/agentic-schedule"
exec uv run --project "$SKILL_DIR" agentic-schedule "$@"
